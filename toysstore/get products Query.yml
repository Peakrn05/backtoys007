info:
  name: get products Query
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/products?search=piano
  params:
    - name: search
      value: piano
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
